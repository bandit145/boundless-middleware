from boundless.mainway_objects import Poi, Location, SubversiveWord, Incident
import boundless.tasks as tasks

# functions for tying biz logic together
def search_messages(data, celery):
    tasks.apply


def retrieve_job(id, celery):
    pass
