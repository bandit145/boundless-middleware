input { rabbitmq { host => "rabbitmq" port => 5672 user => "guest" password => "guest" key => "wiretap" queue => "wiretap"} }
output {
  elasticsearch { hosts => ["elasticsearch:9200"] index => "wiretap_discord-%{+YYYY.MM.dd}" }
  stdout { codec => json }
}